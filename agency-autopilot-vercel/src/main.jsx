import React from "react";
import { createRoot } from "react-dom/client";
import AgencyAutopilot from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AgencyAutopilot />
  </React.StrictMode>
);
